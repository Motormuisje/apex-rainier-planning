repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: pytest -x --tb=short --ignore=tests/browser
        language: system
        pass_filenames: false
        always_run: true
